import React, { Component, Fragment } from 'react';

export default class NotFoundPage extends Component {
  static propTypes = {
  };

  render() {
    return <Fragment>
      <h1>This is not valid page</h1>
    </Fragment>
  }
}
